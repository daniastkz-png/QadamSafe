# Firebase Service Account (JSON string - get from Firebase Console)
# Go to: Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Then paste the entire JSON content as a single line here
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"qadamsafe",...}

# OpenAI API Key (for gpt-4o-mini: generate-scenario, chat)
# https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# JWT Secret (generate a secure random string)
JWT_SECRET=your-super-secret-jwt-key-here

# Port (Render will set this automatically)
PORT=3001
