# QadamSafe - Cybersecurity Education Platform

Платформа для обучения кибербезопасности через интерактивные сценарии.

## 🚀 Быстрый старт

### Вариант 1: Запуск через корневой скрипт (рекомендуется)

1. Установите зависимости для всех проектов:
```bash
npm run install:all
```

2. Установите `concurrently` (если еще не установлен):
```bash
npm install
```

3. Запустите оба проекта одновременно:
```bash
npm run dev
```

Это запустит:
- **Backend** на `http://localhost:3000`
- **Frontend** на `http://localhost:5173`

### Вариант 2: Запуск в отдельных терминалах

**Терминал 1 - Backend:**
```bash
cd backend
npm install  # если еще не установлены зависимости
npm run dev
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm install  # если еще не установлены зависимости
npm run dev
```

## 📋 Требования

- Node.js 18+ 
- npm или yarn
- PostgreSQL (для backend)
- Firebase (опционально, для аутентификации)

## ⚙️ Настройка переменных окружения

### Backend

Создайте файл `backend/.env`:

```env
PORT=3000
NODE_ENV=development
DATABASE_URL="postgresql://user:password@localhost:5432/qadamsafe"
JWT_SECRET="your-secret-key-change-in-production"
CORS_ORIGIN="http://localhost:5173"

# Firebase (опционально)
FIREBASE_PROJECT_ID="your-project-id"
FIREBASE_PRIVATE_KEY="your-private-key"
FIREBASE_CLIENT_EMAIL="your-client-email"
```

### Frontend

Создайте файл `frontend/.env`:

```env
VITE_API_URL=http://localhost:3000
```

## 🗄️ База данных

1. Убедитесь, что PostgreSQL запущен
2. Создайте базу данных:
```sql
CREATE DATABASE qadamsafe;
```

3. Примените миграции:
```bash
cd backend
npx prisma migrate dev
```

4. (Опционально) Заполните тестовыми данными:
```bash
npm run seed
```

## 📦 Доступные команды

### В корне проекта:
- `npm run dev` - Запустить backend и frontend в режиме разработки
- `npm run build` - Собрать production версию
- `npm run install:all` - Установить зависимости для всех проектов

### Backend (`cd backend`):
- `npm run dev` - Запуск в режиме разработки с hot-reload
- `npm run build` - Собрать TypeScript
- `npm start` - Запустить production версию
- `npm run migrate:dev` - Применить миграции
- `npm run prisma:studio` - Открыть Prisma Studio

### Frontend (`cd frontend`):
- `npm run dev` - Запуск dev сервера (Vite)
- `npm run build` - Собрать production версию
- `npm run preview` - Предпросмотр production сборки
- `npm run lint` - Проверить код линтером

## 🌐 Порты

- **Backend API**: `http://localhost:3000`
- **Frontend**: `http://localhost:5173`
- **Health Check**: `http://localhost:3000/health`

## 📁 Структура проекта

```
QadamSafe 2/
├── backend/          # Express.js API сервер
│   ├── src/
│   ├── prisma/       # Схема базы данных
│   └── dist/         # Скомпилированный код
├── frontend/         # React + Vite приложение
│   ├── src/
│   └── public/
└── functions/        # Firebase Functions
```

## 🐛 Решение проблем

### Порт уже занят
Если порт 3000 или 5173 занят, измените в `.env` файлах соответствующие значения.

### Ошибки базы данных
Убедитесь, что:
- PostgreSQL запущен
- База данных создана
- Миграции применены (`npm run migrate:dev`)

### Ошибки Firebase
Firebase не обязателен для работы. Если не настроен, приложение будет работать с обычной JWT аутентификацией.

## 📚 Дополнительная информация

См. [IMPROVEMENTS.md](./IMPROVEMENTS.md) для списка улучшений и рекомендаций.

