# Backend API URL (for /api/ai/generate-scenario, /api/ai/chat)
# Local development: backend must run on 3001 (cd backend && npm start)
# Copy to .env.local and set:
VITE_API_URL=http://localhost:3001

# Production build uses .env.production (VITE_API_URL=https://qadamsafe-api.onrender.com)
